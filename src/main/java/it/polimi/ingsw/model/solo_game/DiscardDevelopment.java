package it.polimi.ingsw.model.solo_game;

import it.polimi.ingsw.model.Game;

/**
 * Discards a card in a `DevelopmentCard` deck
 */
public class DiscardDevelopment implements SoloTokenStrategy {
    @Override
    public void use() {
//        TODO dev cards are decks, we need to pick a deck
//        Game.getdevelopmentCardDecks().drawLastCard();
    }
}
